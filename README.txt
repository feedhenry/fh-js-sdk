the jssdk is now build using grunt.

do an npm install

To build it use grunt concat

To add new files edit the grunt.js file under concat

Thirdparty libs go under libs and our modules go under src.

There are qunit tests. You will need to install phantomjs to run these tests. There is also no a job on denzil for building the project.